import 'ext/ext_string.dart';
import 'ext/ext_int.dart';

void main() {
  // String name = "hello world";
  // print(name.capitalizefirst());
  // print(name.capitalize());
  // print(name.camelCase());
  // print(name.isAlphabetOnly());
  // print(name.isAudioFileName());
  // print(name.isEmail());
  // print(name.isBool());
  // print(name.isPalindrom());
  // print(name.isURL());
  // print(name.isPhoneNumber());
  // print(name.isHexadecimal());
  // print(name.isEmail());

  int number = 232;
  print(number.isPalindrome());



}